{
    "name": "allergie-app",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "generate-config": "tsc src/generate-config.ts && node dist/generate-config.js",
        "build": "npm run generate-config && tsc",
        "start:electron": "npm run build && electron dist/main.js",
        "start:liveserver": "npm run build && live-server src/views --port=4000",
        "start": "node start.js"
    },
    "devDependencies": {
        "@types/express": "^5.0.1",
        "@types/node": "^22.14.1",
        "concurrently": "^8.2.2",
        "dotenv": "^16.5.0",
        "ts-node": "^10.9.2",
        "typescript": "^5.8.3"
    },
    "dependencies": {
        "allergie-app": "file:",
        "concurrently": "^8.2.2",
        "electron": "^35.2.0",
        "express": "^5.1.0"
    }
}
