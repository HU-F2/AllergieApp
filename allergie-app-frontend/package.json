{
    "name": "frontend",
    "version": "1.0.0",
    "main": "index.js",
    "scripts": {
        "watch": "npx nodemon --watch 'src/**/*.ts' --exec 'tsc' --ext ts",
        "start": "live-server --port=8080",
        "dev": "concurrently \"npm run watch\" \"npm run start\""
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "description": "",
    "devDependencies": {
        "concurrently": "^9.1.2",
        "live-server": "^1.2.2",
        "nodemon": "^3.1.9",
        "typescript": "^5.7.3"
    },
    "dependencies": {
        "frontend": "file:"
    }
}
